import { ConcatenarPruebaPipe } from './concatenar-prueba.pipe';

describe('ConcatenarPruebaPipe', () => {
  it('create an instance', () => {
    const pipe = new ConcatenarPruebaPipe();
    expect(pipe).toBeTruthy();
  });
});
